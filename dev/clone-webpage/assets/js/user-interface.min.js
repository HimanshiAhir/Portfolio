// tooltip
    const tooltipTriggerList = document.querySelectorAll('[data-bs-toggle="tooltip"]')
    const tooltipList = [...tooltipTriggerList].map(tooltipTriggerEl => new bootstrap.Tooltip(tooltipTriggerEl))

// mobile menu
document.addEventListener('DOMContentLoaded', function () {
    var navbarToggler = document.querySelector('.navbar-toggler');
    var lHeader = document.querySelector('.l-header');

    navbarToggler.addEventListener('click', function () {
    lHeader.classList.toggle('active');
    });
});


// data table

$(document).ready(function() {
    // Initialize DataTable with your table ID
    var tnxTable = new DataTable('#tnxTable', {
        ordering: false,
        searching: false,
        info: false,
        lengthChange: false,
        pageLength: 15,
    });

    var topTable = new DataTable('#topTable', {
        ordering: false,
        searching: false,
        info: false,
        lengthChange: false,
        pageLength: 15,
    });
});




// chart

// Get the canvas element 
var ctx = document.getElementById('topHoldersChart').getContext('2d'); 
 
var myDoughnutChart = new Chart(ctx, { 
    type: 'doughnut', 
    data: { 
        labels: ['bc1q...rna6', 'bc1q...n2ky', 'bc1q...raht', 'bc1q...nhku', 'bc1q...lgl9', 'bc1q...s0j3', 'bc1q...4f2w', 'bc1q...4lls', 'bc1q...n53k', 'Other Address'], 
        datasets: [{ 
            label: 'My Doughnut Chart',  
            data: [6.65, 0.35, 0.26, 0.25, 0.23, 0.20, 0.18, 0.17, 0.17, 92],  
            backgroundColor: [ 
                'rgb(250, 239, 20)', // Yellow 
                'rgb(238, 95, 132)', // Pink 
                'rgb(145, 131, 233)', // purple 
                'rgb(247, 184, 68)', // orange 
                'rgb(67, 191, 181)', // green 
                'rgb(215, 119, 63)', // red 
                'rgb(193, 232, 70)', // dark green 
                'rgb(73, 151, 206)', // blue 
                'rgb(110, 161, 62)', // green 
                'rgb(69, 172, 203)' // Other Address 
            ], 
            hoverOffset: 4,
            borderWidth: 1
            
        }] 
    },
    options: { 
        responsive: true, 
        aspectRatio: 2, 
        plugins: { 
            legend: { 
                position: 'top', 
                labels: { 
                    color: 'white' 
                } 
            }, 
            title: { 
                display: true 
            } 
        } 
    } 
});
